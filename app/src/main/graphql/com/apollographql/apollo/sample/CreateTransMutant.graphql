mutation CreateTransMutant($trans : TransactionInput!){
    createTransaction	(newTransaction: $trans){
        transactionId
        totalAmount
        received
        changed
        branchId
        details{
            itemId
        }
    }
}